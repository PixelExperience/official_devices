language: minimal

dist: bionic

services:
  - docker

script:
  - docker build -f Dockerfile.maintainers . -t ci:latest
  - docker run -it -e TELEGRAM_TOKEN=${TELEGRAM_TOKEN} -e PULL_REQUEST_NUMBER=${PULL_REQUEST_NUMBER} -e GH_PERSONAL_TOKEN=${GH_PERSONAL_TOKEN} ci
