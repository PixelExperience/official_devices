version: v1.0
name: PixelExperience Continuous Integration
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: JSON Checker and Linter
    task:
      jobs:
      - name: Job
        commands:
          - checkout
          - echo "Testing PR"
      secrets:
        - name: Pixel-CI
